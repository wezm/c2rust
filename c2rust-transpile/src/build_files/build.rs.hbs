#[cfg(all(unix, not(target_os = "macos")))]
fn main() {
    // add unix dependencies here below
    // println!("cargo:rustc-flags=-l readline");
}

#[cfg(all(unix, target_os = "macos"))]
fn main() {
    // add macos dependencies below
    // println!("cargo:rustc-flags=-l edit");
}
